<%= form_for(@sabstract) do |f| %>
  <% if @sabstract.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sabstract.errors.count, "error") %> prohibited this sabstract from being saved:</h2>

      <ul>
      <% @sabstract.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :department_id %><br>
    <%= f.select :department_id, Department.all.collect {|dept| [dept.title, dept.id]} %>
  </div>
  <div class="actions">
    <%= f.submit "Create Abstract" %>
  </div>
<% end %>
